import React from 'react';
import classes from "./icon.module.css";

const Icon = ({style}) => {
    return (
        <div className={classes.icon} style={style}>

        </div>
    )
};

export default Icon;